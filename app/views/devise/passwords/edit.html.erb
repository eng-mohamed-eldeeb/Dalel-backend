<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #B27552;
      color: #fff;
      margin: 0;
      padding: 0;
    }

    .container {
      max-width: 600px;
      margin: 20px auto;
      padding: 20px;
      background-color: #fff;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    h1, h2 {
      color: #B27552;
    }

    p {
      margin-bottom: 16px;
    }

    label {
      font-weight: bold;
    }

    em {
      color: #6c757d;
    }

    input {
      width: 100%;
      padding: 10px;
      margin: 8px 0;
      box-sizing: border-box;
    }

    .field {
      margin-bottom: 20px;
    }

    .actions {
      text-align: center;
    }

    .actions input {
      background-color: #B27552;
      color: #fff;
      border: none;
      padding: 10px 20px;
      font-size: 16px;
      cursor: pointer;
      border-radius: 4px;
    }

    .actions input:hover {
      background-color: #8F4B2B;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>Change your password</h2>

    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>
      <%= f.hidden_field :reset_password_token %>

      <div class="field">
        <label for="password">New password</label>
        <% if @minimum_password_length %>
          <p><em>(<%= @minimum_password_length %> characters minimum)</em></p>
        <% end %>
        <input type="password" name="<%= resource_name %>[password]" id="password" autofocus="true" autocomplete="new-password">
      </div>

      <div class="field">
        <label for="password_confirmation">Confirm new password</label>
        <input type="password" name="<%= resource_name %>[password_confirmation]" id="password_confirmation" autocomplete="new-password">
      </div>

      <div class="actions">
        <%= f.submit "Change my password" %>
      </div>
    <% end %>

  </div>
</body>
</html>
